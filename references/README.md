# Reference source snapshots

This directory contains source snapshots used by the OA project:

- `cx/`: `/home/liujunnan/gs-ai/cx`
- `digital-employee-platform/`: `/home/liujunnan/gs-ai/digital-employee-platform`
- `QwenPaw+cowagent/`: `/home/liujunnan/gs-ai/QwenPaw+cowagent`

The snapshots preserve source layout and are included for local reference. Git metadata,
dependency installations, build output, caches, runtime storage, credentials, archives,
and large generated assets are excluded. The CowAgent snapshot is the local deployment
and patch source available in the workspace; it is not represented as a complete upstream
runtime checkout.
