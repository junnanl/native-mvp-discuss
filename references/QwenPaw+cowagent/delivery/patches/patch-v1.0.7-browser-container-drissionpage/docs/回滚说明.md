# 回滚说明：patch-v1.0.7-browser-container-drissionpage

```bash
cd patch-v1.0.7-browser-container-drissionpage
TARGET_ROOT=/opt/cowagent-offline ./rollback.sh
```

## 回滚动作

1. 从最近一次安装备份恢复：`mcp.json`、`skills_config.json`、Skill 目录、`toolpacks/mcp/browser-dp`、`toolpacks/python/drissionpage`、`compose/docker-compose.browser.yml`（含安装前不存在的情况）；
2. 兜底清理 mcp.json：移除 browser-dp 条目、browser-use 恢复 `disabled: false`；
3. 从 `compose/.env` 的 `COMPOSE_FILE` 链中摘除 `docker-compose.browser.yml`；
4. 停止并移除 `<container>-browser` 容器；
5. `docker compose up -d`（基线两文件）重启，等待 Web 控制台就绪。

## 保留不删

- `storage/browser/profile`：登录态，回滚后重装补丁可继续使用；
- `storage/browser/downloads`、`storage/browser/uploads`：已下载/待上传文件；
- browser-use toolpack 与其 site-packages：全程未改动。

## 回滚后状态

回到 1.1.0 基线行为（browser-use MCP 启用）。注意：browser-use 原有的"浏览器随 MCP 进程退出被杀"问题会随之回来，这是预期行为。
