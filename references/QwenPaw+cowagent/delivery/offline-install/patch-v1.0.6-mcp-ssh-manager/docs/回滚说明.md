# v1.0.6 回滚说明

执行：

```bash
bash rollback.sh
```

脚本恢复更新前的 MCP Toolpack、`storage/cow/mcp-ssh-manager`、`storage/cow/mcp.json`、SSH 管理 Skill 和 `skills_config.json`，随后重启并验证 Web 服务。运行期新增的 SSH 连接随 `storage/cow/mcp-ssh-manager` 回退到安装前快照；不会修改模型配置、OfficeCLI、HTML Report、对话、知识库或上传文件。
