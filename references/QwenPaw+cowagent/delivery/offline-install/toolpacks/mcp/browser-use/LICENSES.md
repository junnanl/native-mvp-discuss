# Browser-use Toolpack License Inventory

The primary upstream projects are distributed under the MIT License:

- `browser-use 0.13.8`: `browser-use` package metadata and `LICENSE`
- `browser-harness 0.1.9`: `browser-harness` package metadata and `LICENSE`

The bundled Python dependencies retain their upstream license files under:

```text
../python/browser-use/site-packages/*dist-info/licenses/
```

This inventory is shipped with the offline package. The toolpack does not
modify upstream package license text.
