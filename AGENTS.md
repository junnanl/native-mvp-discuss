# AI-native OA 流 · 开发规则

本文件只管**执行纪律**。产品判断、架构决定、UI 规格全部在方案文档里，此处不重复、不解释、不留余地。

---

## 1. 唯一方案来源

- 所有实现决定以 `docs/AI-native-OA流-方案-v5.1.md` 为准。
- **文档没写的，先问，不要自己发挥。** 一次性把疑问列全再问，不要一个一个挤。
- 实现中发现方案有问题：**先说、先改文档、再改代码**。不要在代码里偷偷偏离文档。
- 方案文档近两万字，每次任务只读相关章节（指令会点名章节号），不要通读后凭印象实现。

## 2. 分步交付（最重要的一条）

- **每次只做指定的那一步，做完停下，等验收。**
- 不要因为"顺手"而做下一步的事，哪怕只差一点。
- 每一步结束必须有**能演示的东西**，不接受"底层做好了但还看不到"。
- 当前在第几步、这一步的范围，以用户指令为准，不以你的判断为准。

## 3. 边界

- 逐条遵守方案 **§11「明确不做」**。那张表是硬约束，不是建议。
- **默认不新增依赖。** 确有必要先说明理由、等确认，不要先装上再说。
- 不引入 AGPL 及同类传染性许可证的代码。
- 不做方案里没要求的"增强"：审计日志、权限体系、并行分支、条件路由、重试队列、配置中心——这些都很专业，都不要。

## 4. 复用优先

- 开发、修复、重构、依赖选型前读 `skills/ponytail/SKILL.md`，按 YAGNI 与复用优先执行。
- **前端沿用 `frontend/` 现有模板**：待办列表、筛选标签、分页、处理按钮、KPI 卡、系统公告、顶部标签栏，直接复用其实现与样式，不要重做、不要新建一套。
- 动手前先查：模板里有没有现成组件？已装依赖能不能做？没有再写新的。

## 5. 本项目的铁律（违反即返工）

- **组件写死。** AI 只填数据、选组件、出参数，**不生成布局、不输出前端代码**。
- **确定的事不过模型。** 翻页、排序、筛选、提交由组件直接调业务接口，不经过 AI。
- **表单字段和流程节点是数据不是代码。** 任何把字段或节点写进代码的实现都是错的。
- **两个聊天入口后端必须分开**（方案 §6.4）：首页输入框走模型 API，数字员工对话走 Evo-Harness。不许合并成一套逻辑。
- **装饰可以美化真实状态，不能伪造状态**（方案 §6.7）：执行进度必须绑真实事件，不许固定步骤糊弄。
- **AI 查库只读 + 白名单表**，无例外。

## 6. 技能使用（按阶段触发，不串流水线）

- **ponytail**：开发、修复、重构、选型前必读。
- **codebase-design**：确定模块边界与职责时用。
- **code-review**：有自有业务逻辑改动时用。
- **tdd**：只用在有明确正确性要求的地方（流程状态流转、结构化输出校验），**不对 UI 原型强制**。
- **grilling：需求澄清阶段已结束，不要再用它反复追问。** 有疑问按 §1 一次性列出。

## 7. 报告纪律

- 严格区分三种说法：**"我实现了" / "我跑通了" / "我验证了"**。没跑过的不说通过。
- 退出码 0、编译成功、页面能打开，都只是技术条件，不等于验收通过。
- 失败要带**日志内容和文件位置**回报，不要只说"已修复"。
- 不把模型自述、README 声明当作证据。声明兼容不等于运行兼容。

## 8. 术语

| 用词 | 指什么 |
| --- | --- |
| Evo-Harness | 数字员工运行时（上游为 CowAgent / zhayujie） |
| 数字员工 | `agent` 表的一行：prompt + 能力配置 + 运行位 |
| 流程 | `flow_def` 定义、`flow_instance` 实例 |
| 可见范围 | `visible_to` 过滤，**不是**多租户，不做强制访问控制 |
