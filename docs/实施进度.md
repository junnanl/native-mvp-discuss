# 实施进度与证据

## 执行约定

用户已授权连续推进方案第 0–7 步，无需每一步另行确认。内网模型验证不阻塞开发，先使用本地 glm-5.3-flash / deepseek-v4.1-flash。CowAgent 使用 `/home/liujunnan/gs-ai/QwenPaw+cowagent` 的现有运行环境。

## 当前真实状态（2026-09-13）

整体尚未验收。此前消息中的“第 1–3 步完成/闭环完成”超出了证据，不能作为完成依据。

- 文档已移到本目录，前端模板来自 cx/CMI_AH_NEW。
- React 实际复用了模板 MultiWindowTabs、TodoList、StatsCards、Announcements；保留原待办样式、分类、分页、处理按钮。
- Vite `/api` 代理到 127.0.0.1:18000。取消了失败时静默使用假数据的前端回退。
- 已打开的详情和员工标签保持挂载，切换保留状态；关闭全部保留工作台。
- 需求不存在返回 404，推进到完成节点立即完成，再推进返回 409。
- 待办由当前实例节点角色生成，推进后刷新；不再以固定三条待办假装真实流程。
- 确认表单已实际渲染并可保存，但尚未使用 form_def.fields 驱动，不能认定 AI 填表闭环完成。
- 现有员工卡明确标记演示配置；KPI 未接真实统计，显示“—”。

## 本轮验证

- `backend/.venv/bin/python -m unittest discover -s backend/tests -v`：3 个接口行为测试通过，每个修复先观察到失败再改实现。
- `npm run build --prefix frontend`：构建通过。
- `evidence/browser-repair.cjs`：借用现有容器中的 Playwright 和 Chromium 120，不新增浏览器依赖。真实 HTTP 准备 6 条实例，经 Vite 代理验证分页、详情标签、三角色推进、终点完成、分页收缩；页面脚本错误为 0。
- 浏览器截图：`evidence/repair-desktop.png`。该检查未调用真实模型，也没有验证数据库持久化。
- CowAgent 容器 `cowagent-217-verify` 的宿主端口是 19989；GET `http://127.0.0.1:19989/api/version` 返回 2.1.7。先前 9899 超时不能证明服务不可用。与方案记载的 2.1.8 差异需据实际接口处理，不能宣称已经通过 SSE 验证。

## 尚待完成（保留全目标）

1. PostgreSQL/MySQL 持久化：目前只有 schema.sql，实例在内存中，重启会丢失。已询问用户数据库配置归属，现有 PostgreSQL 为 sub2api-postgres，尚未修改它。
2. form_def.fields / flow_def.nodes 全部从数据库读取，移除 Python 的固定字段和默认流程。表单渲染、AI schema 校验、流程推进共享定义。
3. 登录用户、creator_id、visible_to、待阅/提醒/已办真实来源及 KPI 统计；角色与节点一致；开发提交 skill 后上线形成可使用员工。
4. 首页 dispatch 三选一路由；模型认证参数、动态 schema、业务校验和重试；20 次实际调用；提示词配置；SKILL 草稿生成。
5. CowAgent 窄接口的身份/skill 装载与启停、session 映射、SSE 文本和工具事件、三栏对话及新对话。当前仅有未完整验证的消息提交适配器。
6. 五类固定组件、产物画布累积/删除、图谱两个场景、数据分析助手经 DBX 只读白名单查询。尚未实现。
7. 真实数字员工 1–2 个配置验证；不改代码配置报销流程并计时；完整功能与 AI 验收。

## 开发运行

```bash
backend/.venv/bin/python -m uvicorn backend.app.main:app --host 127.0.0.1 --port 18000
npm run dev --prefix frontend
```

默认页面 http://127.0.0.1:15173/ 。既有服务占用 8000，不使用该端口。

### 2026-09-14 追加

- `backend/app/definitions.json` 成为当前表单和流程定义来源；`GET /api/forms` 已提供表单字段定义，AI 填表提示从该定义生成。
- 仍未接数据库持久化；JSON 定义是过渡性的配置来源，不能替代方案要求的数据库。
- CowAgent adapter 已完成登录 Cookie、消息提交与 SSE 代理，默认本地映射端口 19989，版本实测 2.1.7。
- 配置文件已加入第二条“费用报销审批”流程；测试通过 `flow_def_id=2` 创建并按“提需求 → 评审 → 开发”推进完成，未修改 Python 流程逻辑。
- 当前后端仍是内存实例；数据库边界确认后再替换存储实现。
- 已在同一 PostgreSQL 实例创建独立数据库 `ai_native_oa`，未写入 `sub2api`。使用容器网络地址实际初始化 `schema.sql`，6 张表创建成功；`db.create_instance` 插入验证成功。
- PostgreSQL 未发布宿主机端口，运行时需通过 `DATABASE_URL` 指定可达地址；应用暂保留内存测试 adapter，尚未切换所有 API 到数据库。
- 数据库接入准备：独立库 `ai_native_oa` 已创建；修正 identity 主键并完成定义/演示用户种子；`db.create_instance` 已插入验证成功。
- 追加核心 AI 接口：`/api/ai/dispatch`、`/api/ai/resolve-view`、`/api/ai/skill-draft`，并提供 `/api/components` 五类固定组件契约；已做最小 smoke 验证。
- 流程实例 API 已切换 PostgreSQL 持久化：创建、查询、推进、待办读取统一走独立库 `ai_native_oa`；简单重启后查询验证通过。
- 前端已接入 `resolve-view` 查询入口与固定结果卡，数字员工页补齐“过程 / 交流 / 结果”基础三栏。
- 五类固定组件已开始可见渲染：指标、表格、柱状趋势、关系图、文本；`resolve-view` 按自然语言返回组件及数据。
- 数据分析助手已接只读白名单查询：`/api/analysis/query` 仅允许 `flow_instance`、`agent`，前端查询产物支持累积和单张删除。
- 发布接口现在要求关联流程实例已完成；已验证报销流程未完成时返回 409，完成“提需求 → 评审 → 开发 → 报销完成”后才能发布员工。
